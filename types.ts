
export interface SectionData {
  id: string;
  title: string;
  description: string;
  subDescription?: string;
  images: string[];
}

export interface MetricData {
  name: string;
  value: number;
}
